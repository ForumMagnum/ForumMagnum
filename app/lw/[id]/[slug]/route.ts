import { GET } from "../route";

// This is the exact same route; the slug parameter is decorative
// but NextJS doesn't provide a nice affordance for optional path
// parameters.

// eslint-disable-next-line no-barrel-files/no-barrel-files
export { GET };
