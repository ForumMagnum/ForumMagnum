# see http://docs.vulcanjs.org/packages

# matb33:collection-hooks is at the top because it has to initialize before
# lesswrong. This is commented out by default because it's only used in a
# specific development/testing scenario--when you're manually inspecting
# the queries going by to look for N+1 selects or similar problems.
#matb33:collection-hooks

# Accounts packages, for OAuth
accounts-password@1.6.0

# Main LessWrong package
lesswrong

es5-shim@4.8.0
meteortesting:mocha
zodern:standard-minifier-js
typescript@3.7.6
