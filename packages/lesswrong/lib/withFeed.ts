import React from 'react';

export const useFeed = ({resolverName, fragmentName}: {
  resolverName: string,
  fragmentName: FragmentTypeName,
  render: 
}): {
  feed: Array<React.ReactNode>,
  loadMore: any,
} => {
  // TODO
}
