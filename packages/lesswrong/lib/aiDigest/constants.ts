export const DAY_MS = 24 * 60 * 60 * 1000;

/** The reader's free-text digest preferences are capped at this many characters. */
export const AI_DIGEST_PERSONAL_INSTRUCTIONS_MAX_LENGTH = 2_000;

/** The longest window, in days, over which recommendation history can be cleared. */
export const AI_DIGEST_CLEAR_HISTORY_MAX_DAYS = 3_650;
