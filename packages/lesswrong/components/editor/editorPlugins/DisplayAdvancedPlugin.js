
import ToolbarButton from './HeadingsPlugin'

import Plugin from './plugin'
export default class DisplayAdvancedPlugin extends Plugin {
  constructor(props: Props) {
    super(props)
  }
  // name = "DisplayAdvanced"
  // toolbarButtons = [
  //   <ToolbarButton icon={<div>Full</div>} />
  // ]
}
