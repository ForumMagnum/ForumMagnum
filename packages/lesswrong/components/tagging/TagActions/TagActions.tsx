import React from 'react';

const TagActions = ({tag}: {
  tag: TagBasicInfo,
}) => {
  return <>
    { /*Report*/ }
    { /*Edit*/ }
    { /*Subscribe to tagged posts*/ }
    { /*Subscribe to discussion*/ }
  </>
}
